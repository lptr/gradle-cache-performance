include file("common.conf")

defaults = {
    git-checkout = {
        cleanup = "926d3e4"
        build =   "6e840aa"
    }
    cleanup-tasks = ["clean", "assembleDebug"]
    tasks = ["assembleDebug"]
}
