include file("common.conf")

defaults = {
    git-checkout = {
        cleanup = "71ee604"
        build = "6e8593a"
    }
    gradle-args = ["--scan"]
    cleanup-tasks = ["clean", "assembleDebug"]
    tasks = ["assembleDebug"]
}
